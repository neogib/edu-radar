export const PHOTON_CONFIG = {
    apiUrl: "https://photon.komoot.io/api",
    resultsLimit: 5,
    fallbackThreshold: 10,
    locationBiasScale: 0.2,
    fallbackZoom: 14,
    schoolSuggestionLimit: 50,
    schoolFlyToZoom: 16,
    photonFlyToZoom: 12,
} as const
