import type { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
    appId: "ovh.fourone.eduradar",
    appName: "EduRadar",
    webDir: ".output/public",
}

export default config
